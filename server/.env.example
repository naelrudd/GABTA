# =========================================
# GABTA Backend - Environment Configuration
# =========================================
# Copy this file to .env and fill in your values
# NEVER commit .env with real credentials!

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gabta_db
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# JWT Authentication
JWT_SECRET=change_this_to_random_string_min_32_chars
JWT_EXPIRES_IN=24h

# Email Service - RECOMMENDED: Use Mailtrap for development
# Sign up free at https://mailtrap.io and get credentials from inbox settings
EMAIL_HOST=smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USER=your_mailtrap_username
EMAIL_PASS=your_mailtrap_password

# Email Service - Alternative: Gmail (for production)
# Uncomment below and comment Mailtrap lines above
# Requires Gmail App Password: https://myaccount.google.com/apppasswords
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-16-char-app-password

# Frontend URL (used in email verification links)
FRONTEND_URL=http://localhost:3000

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
